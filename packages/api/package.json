{
  "name": "@matt-dunn/api",
  "version": "1.0.0",
  "main": "lib/index.js",
  "module": "src/index.ts",
  "types": "lib/index",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.7"
  },
  "scripts": {
    "build:types": "tsc -b tsconfig.build.json",
    "build:js": "babel ./src -d lib --extensions \".ts,.tsx\" --ignore **/*.story.js,**/*.spec.js --root-mode upward",
    "build": "npm run build:types && npm run build:js"
  }
}
